{
  "name": "my-project-app",
  "version": "1.0.0",
  "scripts": {
  "start": "chmod +x ./node_modules/.bin/json-server && npx json-server db.json --host 0.0.0.0 --port $PORT"
},
 "dependencies": {
    "json-server": "^0.17.4"
  }
}
